\firstname{}
\lastname{<%= lesc(@restaurant.name) %>}
\title{Menu}
\address{<%= lesc(@restaurant.location) %>}
\begin{document}

\maketitle

<% @restaurant.categories.each do |category| %>
	\section{<%= lesc(category.name) %>}
	<% category.items.each do |item| %>
		\cventry{}{<%= item.name %>}{<%= item.content %>}{}{}{}
	<% end %>
<% end %>
